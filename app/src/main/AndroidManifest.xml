<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:versionCode="4"
    android:versionName="3.0"
    package="com.example.comprameste">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_shop"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_shop_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Comprameste">
        <activity
            android:name="com.luizinho_dev.comprameste.Activities.MainActivity"
            android:configChanges="orientation|screenSize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.luizinho_dev.comprameste.Activities.HistorialActivity" />

    </application>

</manifest>